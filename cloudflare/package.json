{
  "name": "goutoujunshi-cloudflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix ../frontend run build:cloudflare",
    "sync:core": "uv run python scripts/sync_core.py",
    "dev": "npm run sync:core && uv run pywrangler dev",
    "bundle:check": "npm run build:frontend && npm run sync:core && wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "deploy:dry": "npm run build:frontend && npm run sync:core && uv run pywrangler deploy --dry-run",
    "deploy": "npm run build:frontend && npm run sync:core && uv run pywrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.136.2"
  }
}
